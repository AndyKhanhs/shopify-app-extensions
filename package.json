{
  "name": "admin-action-proj",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/cli": "3.59.3",
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^9.1.0"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "Admin",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
